This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
appsettings.json
BookStore.Api.csproj
Controllers/BooksController.cs
Controllers/FilesController.cs
Data/BookStoreDbContext.cs
EventHandlers/BookCreatedAuditHandler.cs
EventHandlers/BookCreatedEmailHandler.cs
Events/BookCreatedEvent.cs
Middlewares/CorrelationIdMiddleware.cs
Middlewares/GlobalExceptionMiddleware.cs
Migrations/20260622181626_InitialCreate.cs
Migrations/20260622181626_InitialCreate.Designer.cs
Migrations/20260623115839_AddBookAuditTable.cs
Migrations/20260623115839_AddBookAuditTable.Designer.cs
Migrations/20260623145406_AddMessageIdToBookAudit.cs
Migrations/20260623145406_AddMessageIdToBookAudit.Designer.cs
Migrations/BookStoreDbContextModelSnapshot.cs
Models/BlobFileDto.cs
Models/Book.cs
Models/BookAudit.cs
Models/CreateBookRequestDto.cs
Program.cs
Properties/launchSettings.json
Publishers/AzureServiceBusBookEventPublisher.cs
Publishers/IBookEventPublisher.cs
Repositories/EfBookRepository.cs
Repositories/IBookRepository.cs
Services/BlobStorageService.cs
Services/BookService.cs
Subscribers/AuditSubscriber.cs
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="Migrations/20260623145406_AddMessageIdToBookAudit.cs">
using Microsoft.EntityFrameworkCore.Migrations;

#nullable disable

namespace BookStore.Api.Migrations
{
    /// <inheritdoc />
    public partial class AddMessageIdToBookAudit : Migration
    {
        /// <inheritdoc />
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.AlterColumn<string>(
                name: "EventType",
                table: "BookAudits",
                type: "nvarchar(100)",
                maxLength: 100,
                nullable: false,
                oldClrType: typeof(string),
                oldType: "nvarchar(max)");

            migrationBuilder.AddColumn<string>(
                name: "MessageId",
                table: "BookAudits",
                type: "nvarchar(200)",
                maxLength: 200,
                nullable: false,
                defaultValue: "");

            migrationBuilder.CreateIndex(
                name: "IX_BookAudits_MessageId",
                table: "BookAudits",
                column: "MessageId",
                unique: true);
        }

        /// <inheritdoc />
        protected override void Down(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.DropIndex(
                name: "IX_BookAudits_MessageId",
                table: "BookAudits");

            migrationBuilder.DropColumn(
                name: "MessageId",
                table: "BookAudits");

            migrationBuilder.AlterColumn<string>(
                name: "EventType",
                table: "BookAudits",
                type: "nvarchar(max)",
                nullable: false,
                oldClrType: typeof(string),
                oldType: "nvarchar(100)",
                oldMaxLength: 100);
        }
    }
}
</file>

<file path="Migrations/20260623145406_AddMessageIdToBookAudit.Designer.cs">
// <auto-generated />
using System;
using BookStore.Api.Data;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Infrastructure;
using Microsoft.EntityFrameworkCore.Metadata;
using Microsoft.EntityFrameworkCore.Migrations;
using Microsoft.EntityFrameworkCore.Storage.ValueConversion;

#nullable disable

namespace BookStore.Api.Migrations
{
    [DbContext(typeof(BookStoreDbContext))]
    [Migration("20260623145406_AddMessageIdToBookAudit")]
    partial class AddMessageIdToBookAudit
    {
        /// <inheritdoc />
        protected override void BuildTargetModel(ModelBuilder modelBuilder)
        {
#pragma warning disable 612, 618
            modelBuilder
                .HasAnnotation("ProductVersion", "9.0.15")
                .HasAnnotation("Relational:MaxIdentifierLength", 128);

            SqlServerModelBuilderExtensions.UseIdentityColumns(modelBuilder);

            modelBuilder.Entity("BookStore.Api.Models.Book", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<string>("Author")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.Property<decimal>("Price")
                        .HasColumnType("decimal(18,2)");

                    b.Property<string>("Title")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.HasKey("Id");

                    b.ToTable("Books", (string)null);
                });

            modelBuilder.Entity("BookStore.Api.Models.BookAudit", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<int>("BookId")
                        .HasColumnType("int");

                    b.Property<DateTime>("CreatedAtUtc")
                        .HasColumnType("datetime2");

                    b.Property<string>("EventType")
                        .IsRequired()
                        .HasMaxLength(100)
                        .HasColumnType("nvarchar(100)");

                    b.Property<string>("MessageId")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.HasKey("Id");

                    b.HasIndex("MessageId")
                        .IsUnique();

                    b.ToTable("BookAudits", (string)null);
                });
#pragma warning restore 612, 618
        }
    }
}
</file>

<file path="Controllers/FilesController.cs">
using BookStore.Api.Services;
using Microsoft.AspNetCore.Mvc;

namespace BookStore.Api.Controllers;

[ApiController]
[Route("files")]

public sealed class FilesController : ControllerBase
{
    
private readonly BlobStorageService _blobStorageService;


    public FilesController(BlobStorageService blobStorageService)
    {
        _blobStorageService = blobStorageService;
    }

    [HttpPost("upload")]
    [DisableRequestSizeLimit]
    [IgnoreAntiforgeryToken]

    public async Task<IActionResult> UploadAsync(IFormFile file, CancellationToken cancellationToken)
    {
        
        var blobName = await _blobStorageService.UploadAsync(file, cancellationToken); 

        return Ok(new
        {
            Uploaded = true,
            BlobName = blobName
        });
    }

    [HttpGet]
    public async Task<IActionResult> ListAsync(CancellationToken cancellationToken)
    {
        var files = await _blobStorageService.ListAsync(cancellationToken);
        return Ok(files);
    }

    [HttpGet("{blobName}/download")]
    public async Task<IActionResult> DownloadAsync(string blobName ,CancellationToken cancellationToken)
    {
        try
        {
            var downloadResult = await _blobStorageService.DownloadAsync(blobName, cancellationToken);
            return File(downloadResult.Content.ToStream(), downloadResult.Details.ContentType ?? "application/octet-stream", blobName);
        }

        catch(FileNotFoundException)
        {
            return NotFound(new
            {
                Message = $"File '{blobName}' was not found."
            });
        }
    }

     [HttpDelete("{blobName}")]
    public async Task<IActionResult> DeleteAsync(
        string blobName,
        CancellationToken cancellationToken)
    {
        var deleted = await _blobStorageService.DeleteAsync(
            blobName,
            cancellationToken);

        if (!deleted)
        {
            return NotFound(new
            {
                Message = $"File '{blobName}' was not found."
            });
        }

        return NoContent();
    }

}
</file>

<file path="EventHandlers/BookCreatedAuditHandler.cs">
using BookStore.Api.Events;

namespace BookStore.Api.EventHandlers;

public sealed class BookCreatedAuditHandler
{
    private readonly ILogger<BookCreatedAuditHandler> _logger;

    public BookCreatedAuditHandler(ILogger<BookCreatedAuditHandler> logger)
    {
        _logger = logger;
    }

    public Task HandleAsync(BookCreatedEvent domainEvent)
    {
        _logger.LogInformation(
            "AUDIT HANDLER: Book {BookId} created: {Title}",
            domainEvent.BookId,
            domainEvent.Title);

        return Task.CompletedTask;
    }
}
</file>

<file path="EventHandlers/BookCreatedEmailHandler.cs">
using BookStore.Api.Events;

namespace BookStore.Api.EventHandlers;

public sealed class BookCreatedEmailHandler
{
    private readonly ILogger<BookCreatedEmailHandler> _logger;

    public BookCreatedEmailHandler(ILogger<BookCreatedEmailHandler> logger)
    {
        _logger = logger;
    }

    public Task HandleAsync(BookCreatedEvent domainEvent)
    {
         _logger.LogInformation(
            "EMAIL HANDLER: New book created. Sending email about book {BookId}: {Title}",
            domainEvent.BookId,
            domainEvent.Title);

        return Task.CompletedTask;
    }
}
</file>

<file path="Events/BookCreatedEvent.cs">
namespace BookStore.Api.Events;

public sealed record BookCreatedEvent(
    int BookId,
    string Title,
    string Author,
    decimal Price,
    DateTime OccurredAtUtc);
</file>

<file path="Middlewares/CorrelationIdMiddleware.cs">
using Serilog.Context;

namespace BookStore.Api.Middlewares;

public sealed class CorrelationIdMiddleware
{
    private const string CorrelationIdHeaderName = "X-Correlation-Id";

    private readonly RequestDelegate _next;
    private readonly ILogger<CorrelationIdMiddleware> _logger;

    public CorrelationIdMiddleware(RequestDelegate next, ILogger<CorrelationIdMiddleware> logger)
    {
        _next = next;
        _logger = logger;
        
    }

    public async Task InvokeAsync(HttpContext context)
    {
        var correlationId = context.Request.Headers.TryGetValue(CorrelationIdHeaderName, out var existingCorrelationId)
        ? existingCorrelationId.ToString()
        :Guid.NewGuid().ToString();



        context.Response.Headers[CorrelationIdHeaderName] = correlationId;

        using (Serilog.Context.LogContext.PushProperty("CorrelationId", correlationId))
        {
            _logger.LogInformation("Request started with correlation id {CorrelationId}", correlationId);

            await _next(context);
        }


    }





}
</file>

<file path="Migrations/20260622181626_InitialCreate.cs">
using Microsoft.EntityFrameworkCore.Migrations;

#nullable disable

namespace BookStore.Api.Migrations
{
    /// <inheritdoc />
    public partial class InitialCreate : Migration
    {
        /// <inheritdoc />
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.CreateTable(
                name: "Books",
                columns: table => new
                {
                    Id = table.Column<int>(type: "int", nullable: false)
                        .Annotation("SqlServer:Identity", "1, 1"),
                    Title = table.Column<string>(type: "nvarchar(200)", maxLength: 200, nullable: false),
                    Author = table.Column<string>(type: "nvarchar(200)", maxLength: 200, nullable: false),
                    Price = table.Column<decimal>(type: "decimal(18,2)", nullable: false)
                },
                constraints: table =>
                {
                    table.PrimaryKey("PK_Books", x => x.Id);
                });
        }

        /// <inheritdoc />
        protected override void Down(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.DropTable(
                name: "Books");
        }
    }
}
</file>

<file path="Migrations/20260622181626_InitialCreate.Designer.cs">
// <auto-generated />
using BookStore.Api.Data;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Infrastructure;
using Microsoft.EntityFrameworkCore.Metadata;
using Microsoft.EntityFrameworkCore.Migrations;
using Microsoft.EntityFrameworkCore.Storage.ValueConversion;

#nullable disable

namespace BookStore.Api.Migrations
{
    [DbContext(typeof(BookStoreDbContext))]
    [Migration("20260622181626_InitialCreate")]
    partial class InitialCreate
    {
        /// <inheritdoc />
        protected override void BuildTargetModel(ModelBuilder modelBuilder)
        {
#pragma warning disable 612, 618
            modelBuilder
                .HasAnnotation("ProductVersion", "9.0.15")
                .HasAnnotation("Relational:MaxIdentifierLength", 128);

            SqlServerModelBuilderExtensions.UseIdentityColumns(modelBuilder);

            modelBuilder.Entity("BookStore.Api.Models.Book", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<string>("Author")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.Property<decimal>("Price")
                        .HasColumnType("decimal(18,2)");

                    b.Property<string>("Title")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.HasKey("Id");

                    b.ToTable("Books", (string)null);
                });
#pragma warning restore 612, 618
        }
    }
}
</file>

<file path="Migrations/20260623115839_AddBookAuditTable.cs">
using System;
using Microsoft.EntityFrameworkCore.Migrations;

#nullable disable

namespace BookStore.Api.Migrations
{
    /// <inheritdoc />
    public partial class AddBookAuditTable : Migration
    {
        /// <inheritdoc />
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.CreateTable(
                name: "BookAudits",
                columns: table => new
                {
                    Id = table.Column<int>(type: "int", nullable: false)
                        .Annotation("SqlServer:Identity", "1, 1"),
                    BookId = table.Column<int>(type: "int", nullable: false),
                    EventType = table.Column<string>(type: "nvarchar(max)", nullable: false),
                    CreatedAtUtc = table.Column<DateTime>(type: "datetime2", nullable: false)
                },
                constraints: table =>
                {
                    table.PrimaryKey("PK_BookAudits", x => x.Id);
                });
        }

        /// <inheritdoc />
        protected override void Down(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.DropTable(
                name: "BookAudits");
        }
    }
}
</file>

<file path="Migrations/20260623115839_AddBookAuditTable.Designer.cs">
// <auto-generated />
using System;
using BookStore.Api.Data;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Infrastructure;
using Microsoft.EntityFrameworkCore.Metadata;
using Microsoft.EntityFrameworkCore.Migrations;
using Microsoft.EntityFrameworkCore.Storage.ValueConversion;

#nullable disable

namespace BookStore.Api.Migrations
{
    [DbContext(typeof(BookStoreDbContext))]
    [Migration("20260623115839_AddBookAuditTable")]
    partial class AddBookAuditTable
    {
        /// <inheritdoc />
        protected override void BuildTargetModel(ModelBuilder modelBuilder)
        {
#pragma warning disable 612, 618
            modelBuilder
                .HasAnnotation("ProductVersion", "9.0.15")
                .HasAnnotation("Relational:MaxIdentifierLength", 128);

            SqlServerModelBuilderExtensions.UseIdentityColumns(modelBuilder);

            modelBuilder.Entity("BookStore.Api.Models.Book", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<string>("Author")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.Property<decimal>("Price")
                        .HasColumnType("decimal(18,2)");

                    b.Property<string>("Title")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.HasKey("Id");

                    b.ToTable("Books", (string)null);
                });

            modelBuilder.Entity("BookStore.Api.Models.BookAudit", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<int>("BookId")
                        .HasColumnType("int");

                    b.Property<DateTime>("CreatedAtUtc")
                        .HasColumnType("datetime2");

                    b.Property<string>("EventType")
                        .IsRequired()
                        .HasColumnType("nvarchar(max)");

                    b.HasKey("Id");

                    b.ToTable("BookAudits");
                });
#pragma warning restore 612, 618
        }
    }
}
</file>

<file path="Models/BlobFileDto.cs">
namespace BookStore.Api.Models;

public sealed record BlobFileDto(
    string Name,
    long? Size,
    string? ContentType,
    DateTimeOffset? LastModified);
</file>

<file path="Models/Book.cs">
namespace BookStore.Api.Models
{
    public class Book
    {
        public int Id {get;set;}
        public string Title {get;set;} = string.Empty;
        public string Author {get;set;} = string.Empty;
        public decimal Price {get;set;}
    }
}
</file>

<file path="Models/BookAudit.cs">
namespace BookStore.Api.Models;


public class BookAudit
{
    public int Id {get;set;}
    public int BookId {get;set;}
    public string EventType {get; set;} =string.Empty;
    public DateTime CreatedAtUtc {get;set;}
    public string MessageId { get; set; } = string.Empty;
}
</file>

<file path="Models/CreateBookRequestDto.cs">
namespace BookStore.Api.Models;

public sealed record CreateBookRequestDto(
    string Title,
    string Author,
    decimal Price);
</file>

<file path="Properties/launchSettings.json">
{
  "$schema": "https://json.schemastore.org/launchsettings.json",
  "profiles": {
    "http": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      "applicationUrl": "http://localhost:5099",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    },
    "https": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      "applicationUrl": "https://localhost:7014;http://localhost:5099",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    }
  }
}
</file>

<file path="Publishers/AzureServiceBusBookEventPublisher.cs">
using System.Text.Json;
using Azure.Identity;
using Azure.Messaging.ServiceBus;
using BookStore.Api.Events;
using BookStore.Api.Publishers;


public sealed class AzureServiceBusBookEventPublisher : IBookEventPublisher
{
    private readonly ServiceBusSender _sender;

    private readonly ILogger<AzureServiceBusBookEventPublisher> _logger;

    public AzureServiceBusBookEventPublisher(ILogger<AzureServiceBusBookEventPublisher> logger, IConfiguration configuration)
    {
        _logger = logger;

        var fullyQualifiedNamespace = configuration["ServiceBus:FullyQualifiedNamespace"];

        var queueName = configuration["ServiceBus:BookCreatedQueueName"];

        if (string.IsNullOrWhiteSpace(fullyQualifiedNamespace))
        {
            throw new InvalidOperationException(
                "ServiceBus:FullyQualifiedNamespace is missing.");
        }

        if (string.IsNullOrWhiteSpace(queueName))
        {
            throw new InvalidOperationException(
                "ServiceBus:BookCreatedQueueName is missing.");
        }

        var client = new ServiceBusClient(fullyQualifiedNamespace, new DefaultAzureCredential());

        _sender = client.CreateSender(queueName);

    }


    public async Task PublishBookCreatedAsync(BookCreatedEvent bookCreatedEvent, CancellationToken cancellationToken)
    {
        var json = JsonSerializer.Serialize(bookCreatedEvent);


        var message = new ServiceBusMessage(json)
        {
            ContentType = "application/json",
            Subject = nameof(BookCreatedEvent)
        };

        await _sender.SendMessageAsync(message, cancellationToken);
        _logger.LogInformation("BookCreatedEvent published to Service Bus. Book id: {BookId}", bookCreatedEvent.BookId);









    }

}
</file>

<file path="Publishers/IBookEventPublisher.cs">
using BookStore.Api.Events;

namespace BookStore.Api.Publishers;

public interface IBookEventPublisher
{
    Task PublishBookCreatedAsync(BookCreatedEvent bookCreatedEvent, CancellationToken cancellationToken);
}
</file>

<file path="Services/BlobStorageService.cs">
using Azure.Identity;
using Azure.Storage.Blobs;
using Azure.Storage.Blobs.Models;
using BookStore.Api.Models;

namespace BookStore.Api.Services;

public sealed class BlobStorageService
{
    private const string ContainerName = "uploads";

    private readonly BlobContainerClient _containerClient;
    private readonly ILogger<BlobStorageService> _logger;

    public BlobStorageService(
        IConfiguration configuration,
        ILogger<BlobStorageService> logger)
    {
        _logger = logger;

        var storageAccountName = configuration["Storage:AccountName"];

        if (string.IsNullOrWhiteSpace(storageAccountName))
        {
            throw new InvalidOperationException(
                "Storage account name is missing. Configure Storage:AccountName.");
        }

        var blobServiceUri = new Uri(
            $"https://{storageAccountName}.blob.core.windows.net");

        var blobServiceClient = new BlobServiceClient(
            blobServiceUri,
            new DefaultAzureCredential());

        _containerClient = blobServiceClient.GetBlobContainerClient(ContainerName);
    }

    public async Task<string> UploadAsync(
        IFormFile file,
        CancellationToken cancellationToken)
    {
        if (file.Length == 0)
        {
            throw new InvalidOperationException("File is empty.");
        }

        var safeFileName = Path.GetFileName(file.FileName);

        var blobName = $"{Guid.NewGuid()}-{safeFileName}";

        var blobClient = _containerClient.GetBlobClient(blobName);

        await using var stream = file.OpenReadStream();

        await blobClient.UploadAsync(
            stream,
            new BlobHttpHeaders
            {
                ContentType = file.ContentType
            },
            cancellationToken: cancellationToken);

        _logger.LogInformation(
            "File uploaded to Blob Storage. BlobName: {BlobName}",
            blobName);

        return blobName;
    }

    public async Task<IReadOnlyList<BlobFileDto>> ListAsync(
        CancellationToken cancellationToken)
    {
        var files = new List<BlobFileDto>();

        await foreach (var blobItem in _containerClient.GetBlobsAsync(
            cancellationToken: cancellationToken))
        {
            files.Add(new BlobFileDto(
                Name: blobItem.Name,
                Size: blobItem.Properties.ContentLength,
                ContentType: blobItem.Properties.ContentType,
                LastModified: blobItem.Properties.LastModified));
        }

        return files;
    }

    public async Task<BlobDownloadResult> DownloadAsync(
        string blobName,
        CancellationToken cancellationToken)
    {
        var safeBlobName = Path.GetFileName(blobName);

        var blobClient = _containerClient.GetBlobClient(safeBlobName);

        if (!await blobClient.ExistsAsync(cancellationToken))
        {
            _logger.LogWarning("File {blobName} not found", blobName);
            throw new FileNotFoundException(
                $"Blob '{safeBlobName}' was not found.");
        }

        var downloadResult = await blobClient.DownloadContentAsync(
            cancellationToken);

        return downloadResult.Value;
    }

    public async Task<bool> DeleteAsync(
        string blobName,
        CancellationToken cancellationToken)
    {
        var safeBlobName = Path.GetFileName(blobName);

        var blobClient = _containerClient.GetBlobClient(safeBlobName);
        try
        {
            var response = await blobClient.DeleteIfExistsAsync(
            cancellationToken: cancellationToken);
             if (response.Value)
        {
            _logger.LogInformation(
                "File deleted from Blob Storage. BlobName: {BlobName}",
                safeBlobName);
                
        }

         return response.Value;
        }
        catch (Exception ex)
        {
            _logger.LogWarning(ex,"Deletion failed for blob {BlobName}",safeBlobName);
            return false;
        }
    }
}
</file>

<file path="Subscribers/AuditSubscriber.cs">
using BookStore.Api.Models;
using BookStore.Api.Services;


namespace BookStore.Api.Events;

public sealed class AuditSubscriber
{
    private readonly ILogger<AuditSubscriber> _logger;

    public AuditSubscriber(ILogger<AuditSubscriber> logger)
    {
        _logger = logger;
    }
    
    public void onBookCreated(Book book)
    {
        _logger.LogInformation("AUDIT: Book created: Id: {BookId}, Title: {BookTitle}", book.Id, book.Title);
            
    }





}
</file>

<file path="Controllers/BooksController.cs">
using BookStore.Api.Services;
using BookStore.Api.Models;
using Microsoft.AspNetCore.Mvc;
using System.Diagnostics;

namespace BookStore.Api.Controllers;

[ApiController]
[Route("books")]
public sealed class BooksController : ControllerBase
{
    private const string ActivitySourceName = "BookStore.Api";

    private static readonly ActivitySource ActivitySource = new(ActivitySourceName);

    private readonly BookService _bookService;
    private readonly ILogger<BooksController> _logger;

    public BooksController(
        BookService bookService,
        ILogger<BooksController> logger)
    {
        _bookService = bookService;
        _logger = logger;
    }

    [HttpGet]
    public async Task<IActionResult> GetAllAsync(
        CancellationToken cancellationToken)
    {
        var books = await _bookService.GetAllAsync(cancellationToken);

        return Ok(books);
    }

    [HttpGet("{id:int}")]
    public async Task<IActionResult> GetByIdAsync(
        int id,
        CancellationToken cancellationToken)
    {
        var book = await _bookService.GetByIdAsync(
            id,
            cancellationToken);

        if (book is null)
        {
            return NotFound(new
            {
                Message = $"Book with id {id} was not found."
            });
        }

        return Ok(book);
    }

    [HttpPost]
    public async Task<IActionResult> CreateAsync(
        CreateBookRequestDto request,
        CancellationToken cancellationToken)
    {
        using var activity = ActivitySource.StartActivity("BookCreated");

        var book = await _bookService.CreateAsync(
            request.Title,
            request.Author,
            request.Price,
            cancellationToken);

        activity?.SetTag("book.id", book.Id);
        activity?.SetTag("book.title", book.Title);
        activity?.SetTag("book.author", book.Author);

        _logger.LogInformation(
            "Book created through BooksController. BookId: {BookId}",
            book.Id);

        return Created($"/books/{book.Id}", book);
    }
};
</file>

<file path="Data/BookStoreDbContext.cs">
using BookStore.Api.Models;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Api.Data;

public sealed class BookStoreDbContext : DbContext
{
    public BookStoreDbContext(DbContextOptions<BookStoreDbContext> options) : base(options)
    {
        
    }

    public DbSet<Book> Books => Set<Book>();
    public DbSet<BookAudit> BookAudits => Set<BookAudit>();
     protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Book>(entity =>
        {
            entity.ToTable("Books");

            entity.HasKey(book => book.Id);

            entity.Property(book => book.Title)
                .IsRequired()
                .HasMaxLength(200);

            entity.Property(book => book.Author)
                .IsRequired()
                .HasMaxLength(200);

            entity.Property(book => book.Price)
                .HasColumnType("decimal(18,2)");

        });

        modelBuilder.Entity<BookAudit>(entity =>
            {
                entity.ToTable("BookAudits");

                entity.HasKey(audit => audit.Id);

                entity.Property(audit => audit.BookId)
                    .IsRequired();

                entity.Property(audit => audit.EventType)
                    .IsRequired()
                    .HasMaxLength(100);

                entity.Property(audit => audit.CreatedAtUtc)
                    .IsRequired();

                entity.Property(audit => audit.MessageId)
                    .IsRequired()
                    .HasMaxLength(200);

                entity.HasIndex(audit => audit.MessageId)
                    .IsUnique();
            });
    }
    



}
</file>

<file path="Middlewares/GlobalExceptionMiddleware.cs">
using System.Text.Json;
using BookStore.Api.Repositories;

namespace BookStore.Api.Middlewares;

public sealed class GlobalExceptionMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<GlobalExceptionMiddleware> _logger;

    public GlobalExceptionMiddleware(
    RequestDelegate next,
    ILogger<GlobalExceptionMiddleware> logger)
        {
            _next = next;
            _logger = logger;
        }

     public async Task InvokeAsync(HttpContext context)
    {
        try
        {
            await _next(context);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex,"Unhandled exception occurred");

            var statusCode = ex switch
            {
                ArgumentException => StatusCodes.Status400BadRequest,
                KeyNotFoundException => StatusCodes.Status404NotFound,
                _ => StatusCodes.Status500InternalServerError
            };

            context.Response.StatusCode = statusCode;

            await context.Response.WriteAsJsonAsync(
                new
                {
                    Message = ex.Message,
                    StatusCodes = statusCode
                });
        }
    }


}
</file>

<file path="Migrations/BookStoreDbContextModelSnapshot.cs">
// <auto-generated />
using System;
using BookStore.Api.Data;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Infrastructure;
using Microsoft.EntityFrameworkCore.Metadata;
using Microsoft.EntityFrameworkCore.Storage.ValueConversion;

#nullable disable

namespace BookStore.Api.Migrations
{
    [DbContext(typeof(BookStoreDbContext))]
    partial class BookStoreDbContextModelSnapshot : ModelSnapshot
    {
        protected override void BuildModel(ModelBuilder modelBuilder)
        {
#pragma warning disable 612, 618
            modelBuilder
                .HasAnnotation("ProductVersion", "9.0.15")
                .HasAnnotation("Relational:MaxIdentifierLength", 128);

            SqlServerModelBuilderExtensions.UseIdentityColumns(modelBuilder);

            modelBuilder.Entity("BookStore.Api.Models.Book", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<string>("Author")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.Property<decimal>("Price")
                        .HasColumnType("decimal(18,2)");

                    b.Property<string>("Title")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.HasKey("Id");

                    b.ToTable("Books", (string)null);
                });

            modelBuilder.Entity("BookStore.Api.Models.BookAudit", b =>
                {
                    b.Property<int>("Id")
                        .ValueGeneratedOnAdd()
                        .HasColumnType("int");

                    SqlServerPropertyBuilderExtensions.UseIdentityColumn(b.Property<int>("Id"));

                    b.Property<int>("BookId")
                        .HasColumnType("int");

                    b.Property<DateTime>("CreatedAtUtc")
                        .HasColumnType("datetime2");

                    b.Property<string>("EventType")
                        .IsRequired()
                        .HasMaxLength(100)
                        .HasColumnType("nvarchar(100)");

                    b.Property<string>("MessageId")
                        .IsRequired()
                        .HasMaxLength(200)
                        .HasColumnType("nvarchar(200)");

                    b.HasKey("Id");

                    b.HasIndex("MessageId")
                        .IsUnique();

                    b.ToTable("BookAudits", (string)null);
                });
#pragma warning restore 612, 618
        }
    }
}
</file>

<file path="Repositories/EfBookRepository.cs">
using BookStore.Api.Data;
using BookStore.Api.Models;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Api.Repositories;

public sealed class EfBookRepository : IBookRepository
{
    private readonly BookStoreDbContext _context;
    private readonly ILogger<EfBookRepository> _logger;

    public EfBookRepository(
        BookStoreDbContext context,
        ILogger<EfBookRepository> logger)
    {
        _context = context;
        _logger = logger;
    }

    public async Task<IReadOnlyList<Book>> GetAllAsync(
        CancellationToken cancellationToken)
    {
        var books = await _context.Books
            .AsNoTracking()
            .OrderBy(book => book.Id)
            .ToListAsync(cancellationToken);

        _logger.LogInformation(
            "Loaded all books from database. Count: {BooksCount}",
            books.Count);

        return books;
    }

    public async Task<Book?> GetByIdAsync(
        int id,
        CancellationToken cancellationToken)
    {
        _logger.LogInformation(
            "Loading book from database. BookId: {BookId}",
            id);

        return await _context.Books
            .AsNoTracking()
            .FirstOrDefaultAsync(
                book => book.Id == id,
                cancellationToken);
    }

    public async Task<Book> AddAsync(
        Book book,
        CancellationToken cancellationToken)
    {
        _context.Books.Add(book);

        await _context.SaveChangesAsync(cancellationToken);

        _logger.LogInformation(
            "Book saved to database. BookId: {BookId}, Title: {Title}",
            book.Id,
            book.Title);

        return book;
    }
}
</file>

<file path=".gitignore">
# .NET build output
bin/
obj/

# User-specific files
*.user
*.suo
*.rsuser

# VS Code
.vscode/

publish/


# Rider / JetBrains
.idea/

# Logs
logs/
*.log

# Environment files
.env
.env.*

# OS files
.DS_Store
Thumbs.db

# Test results
TestResults/
coverage/
*.coverage
*.coveragexml

# NuGet
*.nupkg
packages/

appsettings.Development.json
</file>

<file path="Repositories/IBookRepository.cs">
using BookStore.Api.Models;

namespace BookStore.Api.Repositories;

public interface IBookRepository
{
    Task<IReadOnlyList<Book>> GetAllAsync(CancellationToken cancellationToken);

    Task<Book?> GetByIdAsync(int id, CancellationToken cancellationToken);

    Task<Book> AddAsync(Book book, CancellationToken cancellationToken);
}
</file>

<file path="appsettings.json">
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

 "MyApp": {
    "WelcomeMessage": "Hello from appsettings.json"
  },

  "ConnectionStrings": {
  "DefaultConnection": "Server=localhost;Database=BookStoreDb;User Id=local;Password=local-password;"
},

  "Serilog": {
    "Using": [ "Serilog.Sinks.Console", "Serilog.Sinks.File" ],
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.AspNetCore": "Warning"
      }
    },
    "Enrich": [ "FromLogContext" ],
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "outputTemplate": "{Timestamp:HH:mm:ss} [{Level:u4}] [{CorrelationId}] {Message:lj}{NewLine}{Exception}"
        }
      },
      {
        "Name": "File",
        "Args": {
          "path": "logs/bookstore-.txt",
          "rollingInterval": "Day",
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} [{Level:u3}] [{CorrelationId}] {Message:lj}{NewLine}{Exception}"
        }
      }
    ]
  }
}
</file>

<file path="BookStore.Api.csproj">
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="azure.identity" Version="1.21.0" />
    <PackageReference Include="Azure.Messaging.ServiceBus" Version="7.20.1" />
    <PackageReference Include="Azure.Monitor.OpenTelemetry.AspNetCore" Version="1.5.0" />
    <PackageReference Include="Azure.Storage.Blobs" Version="12.29.0" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="9.0.15">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="9.0.15" />
    <PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
    <PackageReference Include="Serilog.Settings.Configuration" Version="10.0.0" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.1.1" />
    <PackageReference Include="Serilog.Sinks.File" Version="7.0.0" />
    <PackageReference Include="Swashbuckle.AspNetCore" Version="10.2.3" />
  </ItemGroup>

</Project>
</file>

<file path="Services/BookService.cs">
using BookStore.Api.Events;
using BookStore.Api.Models;
using BookStore.Api.Publishers;
using BookStore.Api.Repositories;

namespace BookStore.Api.Services;

public sealed class BookService
{
    private readonly ILogger<BookService> _logger;
    private readonly IBookRepository _bookRepository;
    private readonly IBookEventPublisher _bookEventPublisher;

    public BookService(
        ILogger<BookService> logger,
        IBookRepository bookRepository,
        IBookEventPublisher bookEventPublisher)
    {
        _logger = logger;
        _bookRepository = bookRepository;
        _bookEventPublisher = bookEventPublisher;
    }

    public async Task<IReadOnlyList<Book>> GetAllAsync(
        CancellationToken cancellationToken)
    {
        _logger.LogInformation("Getting all books");

        return await _bookRepository.GetAllAsync(cancellationToken);
    }

    public async Task<Book?> GetByIdAsync(
        int id,
        CancellationToken cancellationToken)
    {
        _logger.LogInformation("Getting book with id {BookId}", id);

        var book = await _bookRepository.GetByIdAsync(
            id,
            cancellationToken);

        if (book is null)
        {
            _logger.LogWarning(
                "Book with id {BookId} was not found",
                id);
        }

        return book;
    }

    public async Task<Book> CreateAsync(
        string title,
        string author,
        decimal price,
        CancellationToken cancellationToken)
    {
        if (string.IsNullOrWhiteSpace(title))
        {
            _logger.LogWarning("Book title was empty");

            throw new ArgumentException(
                "Title is required.",
                nameof(title));
        }

        if (string.IsNullOrWhiteSpace(author))
        {
            _logger.LogWarning("Book author was empty");

            throw new ArgumentException(
                "Author is required.",
                nameof(author));
        }

        if (price <= 0)
        {
            _logger.LogWarning("Invalid book price {Price}", price);

            throw new ArgumentException(
                "Price must be greater than zero.",
                nameof(price));
        }

        var book = new Book
        {
            Title = title.Trim(),
            Author = author.Trim(),
            Price = price
        };

        var createdBook = await _bookRepository.AddAsync(
            book,
            cancellationToken);

        var domainEvent = new BookCreatedEvent(
            createdBook.Id,
            createdBook.Title,
            createdBook.Author,
            createdBook.Price,
            DateTime.UtcNow);

        await _bookEventPublisher.PublishBookCreatedAsync(
            domainEvent,
            cancellationToken);

        _logger.LogInformation(
            "Book created successfully and event published. BookId: {BookId}, Title: {Title}",
            createdBook.Id,
            createdBook.Title);

        return createdBook;
    }

    public void SimulateError()
    {
        throw new InvalidOperationException("Simulated database error.");
    }
}
</file>

<file path="Program.cs">
using BookStore.Api.EventHandlers;
using BookStore.Api.Events;
using BookStore.Api.Middlewares;
using BookStore.Api.Repositories;
using BookStore.Api.Services;
using Azure.Monitor.OpenTelemetry.AspNetCore;
using Serilog;
using System.Diagnostics;
using BookStore.Api.Data;
using Microsoft.EntityFrameworkCore;
using BookStore.Api.Publishers;

var builder = WebApplication.CreateBuilder(args);

builder.Host.UseSerilog((context, configuration) =>
{
    configuration.ReadFrom.Configuration(context.Configuration);
});

builder.Services.AddScoped<BookService>();
builder.Services.AddScoped<IBookRepository, EfBookRepository>();
builder.Services.AddSingleton<AuditSubscriber>();
builder.Services.AddSingleton<BookCreatedAuditHandler>();
builder.Services.AddSingleton<BookCreatedEmailHandler>();
builder.Services.AddSingleton<BlobStorageService>();

builder.Services.AddDbContext<BookStoreDbContext>(options =>
{
    var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");

    options.UseSqlServer(connectionString);
});
builder.Services.AddSingleton<IBookEventPublisher, AzureServiceBusBookEventPublisher>();
builder.Services.AddControllers();
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

const string ActivitySourceName = "BookStore.Api";
var bookStoreActivitySource = new ActivitySource(ActivitySourceName);

var applicationInsightsConnectionString =
    builder.Configuration["APPLICATIONINSIGHTS_CONNECTION_STRING"];

if (!string.IsNullOrWhiteSpace(applicationInsightsConnectionString))
{
    builder.Services
        .AddOpenTelemetry()
        .UseAzureMonitor(options =>
        {
            options.ConnectionString = applicationInsightsConnectionString;
        })
        .WithTracing(tracing =>
        {
            tracing.AddSource(ActivitySourceName);
        });
}


var app = builder.Build();


app.UseSerilogRequestLogging();
app.UseMiddleware<CorrelationIdMiddleware>();
app.UseMiddleware<GlobalExceptionMiddleware>();

app.UseSwagger();
app.UseSwaggerUI();

app.MapControllers();

app.MapGet("/", () =>
{
    return Results.Ok("BookStore.Api is running from Azure - v2");
});



app.Run();
</file>

</files>
